import { ProjectMemberEntity } from './project_member.entity';

describe('ProjectMemberEntity', () => {
  it('should be defined', () => {
    expect(new ProjectMemberEntity()).toBeDefined();
  });
});
